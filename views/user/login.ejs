<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Dry Fruit Bazar</title>
    <!-- Add Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #fef3e2;
            margin: 0;
            padding: 0;
        }
        .login-container {
            max-width: 400px;
            margin: 50px auto;
            background-color: #dba56f;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            padding: 30px;
        }
        .form-title {
            text-align: center;
            font-weight: bold;
            font-size: 1.5rem;
            margin-bottom: 20px;
            
            color: #000;
        }
        .btn-custom {
            background-color: #000;
            color: #fff;
            font-weight: bold;
        }
        .btn-custom:hover {
            background-color: #333;
        }
        .text-muted {
            text-align: center;
            margin-top: 10px;
            font-size: 14px;
        }
        .text-muted a {
            text-decoration: none;
            color: #000;
        }
        .text-muted a:hover {
            text-decoration: underline;
        }

        .google-signin-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #f5f5f5; 
    border: 1px solid #dcdcdc; 
    border-radius: 50px; 
    padding: 10px 20px; 
    text-decoration: none; 
    font-family: Arial, sans-serif; 
    font-size: 16px;
    color: #555; 
    transition: background-color 0.3s ease; 
  }
  .google-img{
    height: 40px;
  }

  #togglePassword {
      cursor: pointer;
      position: absolute;
      right: 10px;
      top: 38px;
      color: #666;
    }
    /* Wrap the password field and icon in a relative container */
    .password-container {
      position: relative;
    }
    </style>
</head>
<body>
    <div class="login-container">
        <h2 class="mb-4" style="font-size: 24px; text-align: center; font-weight: bold;">DRYFRUIT BAZAR</h2>


        <h3 class="form-title">Login</h3>
        <form action="/login" method="POST">
            <div class="mb-3">
                <label for="email" class="form-label">Email address</label>
                <input type="email" name="email" class="form-control" id="email" placeholder="Enter your email" required>
            </div>
            <div class="mb-3 password-container">
                <label for="password" class="form-label">Password</label>
                <input type="password" name="password" class="form-control" id="password" placeholder="Enter your password" required>
                <i class="fa-solid fa-eye" id="togglePassword"></i>
              </div>
             <%if(locals.message && message.length>0){%>
                <div class="alert alert.danger text.center" style="color:red;">

                    <%=message%>      

                </div>
            <%}%>

            <p class="text-muted mt-3">
                Forgot your password? <a href="/forgotPassword">Reset it here</a>
              </p>
              
            <button type="submit" class="btn btn-custom w-100">Login</button>
            <p class="text-muted mt-3">Don't have an account? <a href="/signup">Sign Up</a></p>
            <hr>
            <!-- <div class="text-center">
                <a href="/auth/google" class="btn btn-danger w-100">Login with Google</a>
            </div> -->

            <div class="social-btns">
                <a href="/auth/google" class="google-signin-btn">
                    <img class="google-img" src="https://developers.google.com/identity/images/g-logo.png" alt="Google Logo">
                    Sign in with Google
                </a>
            </div>
        </form>
    </div>

    <!-- Add Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    

    <script>
        const togglePassword = document.querySelector('#togglePassword');
        const password = document.querySelector('#password');
      
        togglePassword.addEventListener('click', function () {
          
          const type = password.getAttribute('type') === 'password' ? 'text' : 'password';
          password.setAttribute('type', type);
          
          
          this.classList.toggle('fa-eye-slash');
        });
      </script>
      
</body>
</html>
